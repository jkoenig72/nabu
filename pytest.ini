[pytest]
testpaths = test
markers =
    hardware: tests requiring physical audio devices (deselect with -m "not hardware")
    slow: tests that load ML models (deselect with -m "not slow")
    network: tests requiring network access to LLM server (deselect with -m "not network")
pythonpath = .
